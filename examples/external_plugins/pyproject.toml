[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pcvmf-example-plugins"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["PCVMF>=0.3.0"]

[tool.hatch.build.targets.wheel]
packages = ["robot_plugins"]
